{# Card Gen v2 — Prediction badge component.
   The pick/recommendation section at the bottom.
   Context: pick_display (str), disclaimer (str)
#}
<section class="pick">
  <div class="pick-main">{{ pick_display }}</div>
  <div class="pick-disclaimer">{{ disclaimer }}</div>
</section>

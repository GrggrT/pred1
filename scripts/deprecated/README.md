# Deprecated utilities

This folder contains legacy scripts/modules kept for reference.

- Production pipeline jobs live in `app/jobs/`:
  - `sync_data`, `compute_indices`, `build_predictions`, `evaluate_results`, `maintenance`
- Legacy/experimental code was moved here to avoid confusion.

